name: catchlog_android_mvp
description: Offline-first fishing log MVP for Android.
version: 1.0.0+1
publish_to: 'none'

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  provider: ^6.1.2
  drift: ^2.18.0
  path_provider: ^2.1.3
  path: ^1.9.0
  intl: ^0.19.0

flutter:
  uses-material-design: true

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.11
  drift_dev: ^2.18.0
  flutter_lints: ^3.0.2
